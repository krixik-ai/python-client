module:
  name: parser
  models: 
    - name: sentence
    - name: fixed
      params: 
        chunk_size:
          type: int    # define limits and any contstraints in your associated validator
          default: 10
        overlap_size:
          type: int    # define limits and any contstraints in your associated validator
          default: 2
  input: 
    type: text
  output: 
    type: json
  defaults: 
    model: sentence
