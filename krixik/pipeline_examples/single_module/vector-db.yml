pipeline:
  name: vector-db-pipeline
  modules:
  - name: vector-db
    models:
    - name: faiss
    defaults:
      model: faiss
    input:
      type: npy
      permitted_extensions:
      - .npy
    output:
      type: faiss
